# Rust
/target/
**/*.rs.bk

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Data — conformed JSONL vintages ARE committed (the repo is the source of
# truth; a fresh clone must build the site — decision 2026-09-04). Derived
# artifacts are not: parquet is rebuilt by build-facts, pages by the compiler.
data/**/*.parquet
data/pages/
*.duckdb
*.duckdb.wal

# Secrets
.env
.env.*

# OS / editors
.DS_Store
.idea/
/site/
/node_modules/
/.qa/
